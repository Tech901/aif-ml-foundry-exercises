# .env ships with the assignment (students fill it in on their own box).
# Instructors: never commit it with real values.
.env

# Runtime files created by lab.sh / the Prompt Lab.
.testbed.pid
.testbed.log

# Per-student prompts the Prompt Lab saves for the Check buttons to grade.
prompts/

# Python
__pycache__/
*.pyc
