# The course stack ships openai and python-dotenv; lab.sh installs Flask (and anything
# else missing) for the student on first start. This file records all three for a box
# that predates the stack or for running outside Codio.
Flask>=3.0,<4.0
openai>=1.60
python-dotenv>=1.0
