# =============================================================================
# Your endpoint and key for the Foundry Exercises
# =============================================================================
# The "Set up your endpoint" page of the guide fills this file in. The lines
# starting with # are notes for you - the computer skips them. The only two
# lines that matter are the two at the bottom.
#
#   - Paste each value after its = sign, with no spaces around the = and
#     no quote marks.
#   - The endpoint must start with https:// and END WITH A SLASH, exactly
#     as the guide shows it.
#   - The key is a secret and this file is the only place it goes: never in
#     a challenge file, never in a message, never in a screenshot.
#
# The deployment name, memphis-copilot, lives in the code, not here.
# Then press the "Check the endpoint and key" button to verify both values.
#
# NOTE FOR INSTRUCTORS: this file ships blank with the assignment so
# students can fill it in directly. Never commit it with real values in it.
# -----------------------------------------------------------------------------

AI901_ENDPOINT=
AI901_KEY=
